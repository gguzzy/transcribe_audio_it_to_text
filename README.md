# transcribe_audio_it_to_text
Record and transcribe any italian audio into text. It is possible to reproduce the generated text. 

In order to use the repo, please run the script called 'transcribe_audio_to_text.py'

## Environment

To use the aforementioned script, please install the necessary libraries

'pip install faster-whisper sounddevice scipy numpy pyttsx3' 

